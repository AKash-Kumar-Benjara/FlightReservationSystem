//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by FernFlower decompiler)
//

package com.company.frs;

public class Passenger {
    private static int idCounter = 0;
    private int id;
    private Passenger.Address address;
    private Passenger.Contact contact;

    public Passenger(String name, String phoneNumber, String emailId, String addressStreet, String city, String state, int pinCode) {
        this.id = ++idCounter;
        this.address = new Passenger.Address(addressStreet, city, state, pinCode);
        this.contact = new Passenger.Contact(name, phoneNumber, emailId);
    }

    public int getId() {
        return this.id;
    }

    public String getAddress() {
        return "Street: " + this.address.addressStreet + ", City: " + this.address.city + ", State: " + this.address.state;
    }

    public String getContact() {
        return "Name: " + this.contact.name + ", Phone: " + this.contact.phoneNumber + ", Email: " + this.contact.emailId;
    }

    public int getIdCounter() {
        return idCounter;
    }

    private static class Address {
        String addressStreet;
        String city;
        String state;
        int pinCode;

        Address(String addressStreet, String city, String state, int pinCode) {
            this.addressStreet = addressStreet;
            this.city = city;
            this.state = state;
            this.pinCode = pinCode;
        }
    }

    private static class Contact {
        String name;
        String phoneNumber;
        String emailId;

        Contact(String name, String phoneNumber, String emailId) {
            this.name = name;
            this.phoneNumber = phoneNumber;
            this.emailId = emailId;
        }
    }
}
